#include <stdlib.h>
#include <stdio.h>

int main(void)
{
	char username[10] = "";

	printf("Something...\n");
	printf("Give your name: ");
	fgets(username, sizeof(username), stdin);
	snprintf("Hello %s", sizeof(username), username);


	return EXIT_SUCCESS;

}
